// 3 ) do while - exit control loop

#include <stdio.h>
void main()
{
      int count = 0;

      do
      {
            count += 1;
            printf("%d\n", count * count);
      } while (count < 10);

      // count+=1;
      // printf("%d",count*count);

      // count+=1;
      // printf("%d",count*count);

      // count+=1;
      // printf("%d",count*count);
}